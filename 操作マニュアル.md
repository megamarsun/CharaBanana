# CharaBanana 操作マニュアル

## 1. アプリの概要
CharaBanana は、NanoBanana API を利用した画像生成を補助するためのデスクトップ向け GUI アプリです。キャラクター情報・脚本テキスト・生成結果を一元管理し、プロンプト作成から API 送信、ローカル保存までをサポートします。

アプリは Python 製で、`CharaBanana.py` を実行すると Tkinter ベースのウィンドウが起動します。初回起動時に作業用フォルダ `data_work/` と必要な JSON ファイルが自動生成されます。

## 2. 動作環境と準備
- **動作環境**: Windows 10 以降（Python 実行環境がある場合は macOS / Linux でも動作可）
- **Python 実行方法**:
  1. `pip install pillow requests certifi` を実行して依存ライブラリを導入します。
  2. プロジェクトのルートで `python CharaBanana.py` を実行します。
- **配布用バイナリ**: `build.bat` や `banana.ico` は Windows 用配布ビルドで利用します。GUI を起動するだけなら Python 実行で問題ありません。

## 3. 画面構成
起動するとタブ付きの画面が開きます。タブは左から順に以下の役割です。

1. **キャラ管理**: キャラクターの基本情報（ベースプロンプト・参照画像）を登録・編集します。
2. **脚本入力**: シナリオテキストを登録し、キャラ名を置換した最終プロンプトを生成します。
3. **送信**: 最終プロンプトと参照画像を確認・調整し、NanoBanana API へ送信します。また、ローカル保存もここで行います。
4. **設定**: API キーとローカル保存先フォルダを登録します。
5. **生成結果**: 生成済みの画像と対応するプロンプトをプレビュー・管理します。

## 4. 基本的な操作の流れ
1. **設定タブで準備**
   - `NanoBanana APIキー` に Google AI Studio で発行したキーを入力し、`設定を保存` をクリックします。
   - `ローカル保存フォルダ` にプロンプト・画像を書き出す先を設定します（後から変更可）。

2. **キャラ管理タブでキャラ登録**
   - `キャラ名` と `ベースプロンプト`（1 行目は置換に利用するラベル）を入力します。
   - 必要に応じて参照画像を指定し、`保存 新規 or 上書き` をクリックします。
   - 登録済みキャラは左側のリストで選択でき、`削除` ボタンで削除できます。

3. **脚本入力タブでプロンプト生成**
   - `脚本テキスト` にシナリオを貼り付け、`変換して保存` を押すと、テキスト内のキャラ名を登録済みキャラで置換したプロンプトが生成されます。
   - `保存済み脚本` リストで過去の脚本を再選択できます。削除も可能です。
   - 変換後は `検出キャラ` と `生成プロンプト`、参照画像スロットが自動で埋まります。

4. **送信タブで最終調整と送信**
   - `最終プロンプト` と参照画像スロット（最大 3 枚）を確認・編集します。スロットの 1 枚目がベース画像、2・3 枚目が雰囲気参照です。
   - `アスペクト比` を選択（デフォルトは `自動`）。
   - `NanoBananaへ送信` で API にリクエストを送信します。成功すると `生成結果` タブに画像が追加されます。
   - `ローカル保存` を押すと、最終プロンプトと参照画像をローカルフォルダへコピーします。

5. **生成結果タブで履歴管理**
   - 左リストから画像を選ぶとプレビューとプロンプトが表示されます。
   - `エクスポート` で任意の場所にコピー、`削除` でギャラリーと実ファイルから削除できます。
   - `フルサイズ表示` で別ウィンドウに原寸プレビューが開きます。

## 5. データ保存とファイル構成
- アプリは起動ディレクトリ直下に `data_work/` フォルダを作成し、以下の JSON を管理します。
  - `config.json`: API キーとローカル保存先
  - `characters.json`: キャラ情報
  - `scripts.json`: 変換済み脚本
  - `gallery.json`: 生成結果の履歴
- 生成画像は `data_work/outputs/` 配下に `img_0001.png` のように連番保存されます。
- `ローカル保存` 機能を使うと、指定フォルダに `prompt_YYYYmmdd_HHMMSS.txt` と画像コピーが出力されます。

## 6. トラブルシューティング
- **API キーが未設定エラー**: 設定タブで API キーを入力し直し、`設定を保存` を押してください。
- **HTTPS 通信に失敗する**: セキュリティソフトやネットワーク設定で exe の通信がブロックされていないか確認します。証明書ファイルは自動で `REQUESTS_CA_BUNDLE` / `SSL_CERT_FILE` に設定されます。
- **ベース画像が見つからない**: 送信タブでスロット 1 に指定した画像パスが存在するか確認してください。
- **キャラが検出されない**: キャラ名が脚本テキストに含まれているか、キャラ管理タブで登録されているか確認します。最大 3 人まで同時に処理できます。

## 7. よく使うショートカットと補足
- 参照画像選択や保存先選択はすべてファイルダイアログから指定できます。
- 生成結果の JSON レスポンスを確認したい場合、送信エラー時に `data_work/outputs/last_response.json` が出力されます。
- API 送信後に表示される「テスト接続コード」は Google 側への疎通テスト（GET リクエスト）の HTTP ステータスです。

以上で基本操作は完了です。困った際は各タブのエラーメッセージを参照し、設定とファイルパスを確認してください。
